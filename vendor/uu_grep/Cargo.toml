# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
rust-version = "1.88.0"
name = "uu_grep"
version = "0.2.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A Rust implementation of GNU Grep"
homepage = "https://github.com/uutils/grep"
readme = "README.md"
keywords = [
    "grep",
    "uutils",
    "cross-platform",
    "cli",
    "utility",
]
categories = ["command-line-utilities"]
license = "MIT"
repository = "https://github.com/uutils/grep"

[package.metadata.binstall]
pkg-url = "{ repo }/releases/download/v{ version }/{ name }-{ target }{ archive-suffix }"
bin-dir = "{ bin }{ binary-ext }"
pkg-fmt = "txz"

[package.metadata.binstall.overrides.x86_64-pc-windows-msvc]
pkg-fmt = "zip"

[lib]
name = "uu_grep"
path = "src/lib.rs"

[[bin]]
name = "grep"
path = "src/main.rs"

[[test]]
name = "test_grep"
path = "tests/test_grep.rs"

[[bench]]
name = "grep_bench"
path = "benches/grep_bench.rs"
harness = false

[dependencies.clap]
version = "4.5"
features = [
    "wrap_help",
    "cargo",
    "color",
]

[dependencies.glob]
version = "0.3.1"

[dependencies.memchr]
version = "2.7.2"

[dependencies.onig]
version = "~6.5.1"
default-features = false

[dependencies.onig_sys]
version = "69.9.3"
default-features = false

[dependencies.uucore]
version = "0.11.0"

[dependencies.walkdir]
version = "2.5"

[dev-dependencies.criterion]
version = "5.0.0"
package = "codspeed-criterion-compat"

[dev-dependencies.uutests]
version = "0.10.0"

[profile.dist]
lto = "fat"
inherits = "release"

[package.metadata.demi]
maintainer = "demi-runner"
patches = [
    "Standard input is the shell job's, the walk starts from the root resolved against the job's directory and shows paths as named, and temporary file names use the context's process id",
    "Files, streams, the environment, printing and exit go through uucore::context, so a run embedded in a shell job uses the job's working directory, environment, standard streams and cancellation, and exit ends only the utility",
    "The release's tests, benchmarks, examples and CI files are left out",
]

# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.70"
name = "jaq"
version = "3.1.1"
authors = ["Michael Färber <michael.faerber@gedenkt.at>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Just another JSON query tool"
readme = "README.md"
keywords = [
    "json",
    "query",
    "jq",
]
categories = [
    "command-line-utilities",
    "compilers",
    "parser-implementations",
]
license = "MIT"
repository = "https://github.com/01mf02/jaq"

[lib]
path = "src/main.rs"

[dependencies]
jaq-all = { version = "0.3.0", default-features = false, features = ["formats", "std-all"] }
tempfile = "3.3.0"
uucore = "0.11"

[package.metadata.demi]
maintainer = "demi-runner"
patches = [
    "A library entry point (uumain) in place of the binary, without its allocator, line editor, logger, home-directory crate and golden test target",
    "Paths resolve against the shell job's working directory and ~/.jq against its home, through uucore::context",
    "Files, streams, the environment, printing and exit go through uucore::context, so a run embedded in a shell job uses the job's working directory, environment, standard streams and cancellation, and exit ends only the utility",
]

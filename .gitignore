# Dependencies
node_modules/

# Build output — regenerated by `bun run build` (tsdown) and Vite
dist/
*.tsbuildinfo

# Generated API reference — `bun run docs:api` (typedoc)
docs/api/

# Internal-only docs (design history, research, QA acceptance, the readiness plan).
# Kept locally for the team, excluded from the published/open-source repo.
docs/internal/

# Test & coverage
coverage/
.test-cache/

# Bundler / tooling caches
.vite/
.cache/
docs/.vitepress/cache/
docs/.vitepress/dist/

# Packaging artifacts (`npm pack` / `bun pm pack`)
*.tgz

# Logs
*.log

# Local environment & secrets — never commit
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db

# Editors (personal)
.idea/
*.swp
*~

# Claude Code session data
.claude/

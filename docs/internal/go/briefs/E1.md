# Brief E1: backend skeleton and storage

Slot s1 (/home/user/demi-slots/s1), branch go/wp/E1-storage. Rules: _rules.md.

Read first: docs/demi-next/storage.md (whole), docs/demi-next/backend.md
(§ Request paths and responsibilities, § Deployment and user ownership),
docs/package-boundaries.md (`@demicodes/backend` entry, Go dependency graph),
the ledger § E1 (/home/user/demi/docs/internal/go/ledger.md).
Reference (read-only): /home/user/demi-base/packages/backend/src/{storage,backend.ts,lifecycle,dev.ts,testing.ts,testing}
and their tests.

Context: the Go backend replaces the TypeScript backend process as a whole
later (Gate D); until then it grows package by package with Go tests ported by
behavior. It will talk to the existing Rust runner over the runner wire.

Deliver:
- cmd/demi-backend: the composition root (configuration from the same
  environment variables as backend.ts, validated at entry; lifecycle: start,
  graceful stop on SIGINT/SIGTERM, ordered shutdown). Only wiring, no domain
  logic. Wire only what exists after this WP.
- internal/backend/storage: control.sqlite with its migrations, per-conversation
  databases (conversations/<id>.sqlite) with the connection cache storage.md
  describes, blob and change stores, command state, host store, model catalog
  store, user blobs, tree store. Same schemas and file layout as the design
  (and therefore as the TypeScript backend): a data directory is the same
  thing for both. SQLite via modernc.org/sqlite (pure Go, CGO_ENABLED=0), WAL,
  foreign keys, migrations in transactions.
- internal/backend/config, internal/backend/lifecycle as the design names them.
- Test support the later backend WPs need (temporary data directory, store
  constructors).
Owned paths: cmd/demi-backend/**, internal/backend/{storage,config,lifecycle,backendtest}/**, go.mod/go.sum.
Acceptance: Go tests for every ledger § E1 behavior and the storage behaviors
the reference tests guarantee (transactions, migrations order, connection cache
limits, blob collection rules); scripts/go-check.sh clean.

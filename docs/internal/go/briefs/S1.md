# Brief S1: shell core

Slot s1 (/home/user/demi-slots/s1), branch go/wp/S1-shell. Rules: _rules.md in
this directory.

Read first: docs/demi-next/runner.md (§ Load, § Shell jobs, § Cancellation and
completion, § Pipes and output), docs/demi-next/edit-tracking.md (whole),
docs/demi-next/commands.md (§ external command clients),
docs/package-boundaries.md (runner entry), internal/toolctx (the interface you
implement for jobs), the plan's Gate A criteria (A2–A6).
Reference behavior (read-only): /home/user/demi-base/crates/runner/src/shell/
and its tests crates/runner/tests/{shell,edit_tracking,tasks,load,open_files,utilities_cat,utilities_catalog}.rs.
When /home/user/demi/docs/internal/go/ledger.md exists, its § S1 lists the
behaviors you must cover.

Goal: shell jobs on a fork of mvdan.cc/sh (v3, BSD-3) in third_party/mvdan-sh.

Deliver in internal/shell:
- A job API the runner connection (S2) will call: start a job with a script,
  cwd, environment, standard streams, command context and edit context; report
  completion (exit status or cancellation signal, final cwd) only after every
  job-owned goroutine, utility and child process finished; cancel.
- Fresh login shell per job (system profile, first readable user login
  profile, HOME rules from runner.md), command aliases first in PATH, cwd
  restored.
- Exec routing: declared roots → a Dispatcher interface you define (S3
  implements it; test with a fake); utilities → the internal/tools registry
  (create internal/tools/tools.go with an empty family list; test with fake
  utilities through toolctx); everything else → external child processes in a
  process group, killed with the job.
- The job's toolctx.Files and the interpreter's open handler: resolve against
  the job's cwd; every write-capable open, write, whole-file write and
  temp-file publication goes through an EditRecorder interface you define
  (N1 implements it in internal/commandservice/edits; test with a fake that
  logs operations). Redirections of external output forward through the
  recorder as edit-tracking.md says.
- Background lists, `wait`, process substitution, here-docs; `$!` is a
  job-local identifier (runner.md).
- Cancellation per runner.md: interruptible in-process IO (in-memory pipes
  between in-process stages), process groups for children, reported signal.
- Patches to the fork recorded in third_party/mvdan-sh/PATCHES.md.
Owned paths: internal/shell/**, internal/tools/tools.go, third_party/mvdan-sh/**,
go.mod/go.sum.
Acceptance for this WP: Go tests for the ledger's S1 behaviors (until the
ledger exists: the behaviors of the reference tests above), including an
isolation stress test (50 concurrent jobs × 20 rounds, different cwd/env/umask,
pipelines, redirections, background jobs, no cross-talk) and cancellation
tests releasing every goroutine and descriptor within 200 ms (goleak).

# Brief M0+M1: machine manager

Slot s3 (/home/user/demi-slots/s3), branch go/wp/M1-machines. Rules: _rules.md.

Read first: docs/demi-next/managed-hosts.md, docs/cloud-images.md,
docs/managed-hosts-setup.md, docs/gvisor-evaluation.md, docs/package-boundaries.md
(`@demicodes/machines` entry), ledger § M1.
Reference (read-only): /home/user/demi-base/packages/machines and its tests.

M0 first (report it early in your final report): can this container run the
real manager — runsc with --platform=systrap, loop devices, mke2fs/e2fsck/
resize2fs/bsdtar, nftables, network namespaces? Try each with the smallest
command; record exactly what works and what fails and why. Do not install
system packages beyond what `apt-get install` of runsc and e2fsprogs needs; if
installing is impossible, say so.

M1: cmd/demi-machines and internal/machines replacing packages/machines: the
machines wire server (unix socket), device workers, the gVisor lifecycle (runsc
stays an external program, as designed), images, volumes, networking (prefer
netlink/nftables libraries over the nft CLI where the design allows; the
design lists the external programs), provisioner. Wire types come from
internal/contract/machineswire, generated by F4b; F4b merges into go/main
soon: until then build the parts that do not need it, and merge go/main into
your branch when the orchestrator tells you.
Owned paths: cmd/demi-machines/**, internal/machines/**, go.mod/go.sum.
Acceptance: Go tests for every ledger § M1 behavior (the backend's scripted
machine manager stays the test double for backend tests); real-machine checks
only if M0 says they can run here.

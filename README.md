Backup of the Go port's `docs/internal/` (ignored on `go/main`). Never merged.
The working copy is `docs/internal/` in the integration worktree; this branch is
overwritten from it at every checkpoint.

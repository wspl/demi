/** Extra scroll padding above the floating composer so the last block is not flush against it. */
export const COMPOSER_CLEARANCE_PX = 20

/** How far the dock veil extends into the transcript. The fade itself runs through the chip row. */
export const COMPOSER_VEIL_FADE_PX = 32

/** Right inset so the veil does not paint over the transcript scrollbar. */
export const COMPOSER_VEIL_SCROLLBAR_GUTTER_PX = 16

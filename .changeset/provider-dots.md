---
'@demicodes/web-ui': patch
---

A provider's rail dot means attention, for subscriptions and API keys alike: none while it works, yellow while it is not set up, red when it failed or cannot be reached. A signed-in subscription no longer carries a green dot.

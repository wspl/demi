---
'@demicodes/provider': patch
---

Remove the unimplemented `preferBrowser` login option, require `credentialId` on completed login results (all flows import into the pool), and drop stale CLI-spawn wording from the credentials contract and design doc.

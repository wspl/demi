---
"@demicodes/browser-protocol": minor
---

`@demicodes/browser-protocol/live` defines the live browser view's protocol: the frames a user stream carries between the page and the live view module, the video frame header, and the messages each side sends.

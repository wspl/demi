---
'@demicodes/web-ui': minor
---

An active subscription account has a Refresh usage button (`refreshUsage` event, `usage` operation kind) that turns while the vendor is asked. `formatRelativeTime` puts a timestamp in words.

---
'@demicodes/provider': patch
'@demicodes/provider-grok-build': patch
---

A quota probe replaces the windows it names and keeps the ones only an observation reports, as an observation already kept the probed ones; before, a probe dropped every observed window. Grok Build reports no credits window for a plan whose billing names no used amount, limit or percentage.

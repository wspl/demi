---
'@demicodes/web-ui': minor
---

The transcript's tail row says Requesting for as long as the provider is asked, the agent's own retries included. The `retrying` activity kind, the `retrying` session state and `AgentMessageList`'s `retrying` prop are removed, and `providerWaitLabel` takes only the elapsed time.

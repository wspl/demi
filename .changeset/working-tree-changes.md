---
"@demicodes/runner-protocol": minor
"@demicodes/host-remote": minor
"@demicodes/backend": minor
---

The runner answers working-tree requests in process: the uncommitted changes under a directory and a file as the last commit has it, watched between requests. The backend serves them per conversation, with file text for the file view.

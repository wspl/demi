---
'@demicodes/web-ui': minor
'@demicodes/web': minor
---

Build the standalone web prototype with shared gallery sidebar, session composer,
conversation rendering and controls. Share composer presentation with the agent
input, support nested dialog menus and forward input attributes to native inputs.

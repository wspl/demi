---
'@demicodes/web-ui': patch
---

The provider settings offer Test connection to a subscription as they do to an API key; a provider whose requests run on a conversation's Host (`runsOnHost`) has it disabled and says why. An account the vendor names no plan for shows no empty tag.

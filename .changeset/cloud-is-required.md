---
'@demicodes/web-ui': minor
'@demicodes/web': patch
---

Every deployment has Cloud: the product state's `cloud` is never null, the
`no_cloud` answers are gone, and the workspace dialog always offers Cloud.

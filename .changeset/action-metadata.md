---
'@demicodes/agent': minor
'@demicodes/utils': minor
---

Add caller-defined action metadata that crosses agent transports and remains attached to queued actions, prompt construction, reference resolution, tools, lifecycle hooks, and yield wakeups without becoming transcript or provider content.

Export the portable JSON value type used by action metadata, including `bigint` and `Uint8Array` values preserved by Demi transports.

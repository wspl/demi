---
'@demicodes/web-ui': minor
---

`SessionComposer` takes a `hold` line for a state that ends by itself, such as a Cloud reset: the input gives way to a notice with a spinner and returns with its draft when the caller clears it. `SessionNoticeBar` gains `busy`.

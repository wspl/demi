---
'@demicodes/web-ui': patch
---

A tooltip sits above every other layer, so one inside a dialog no longer opens behind it, and a tooltip gives way while a tooltip inside it shows, so a disabled button under a named tip shows its reason alone instead of two tips at once.

---
'@demicodes/web-ui': patch
---

Every name that can be changed in place is also changed by a double-click on it: a conversation's row in the sidebar (`SidebarRow` emits `renameStart`) and an API-key provider's name, as the session header's title already was.

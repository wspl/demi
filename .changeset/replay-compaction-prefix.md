---
'@demicodes/agent': major
---

Run compaction summaries through `AgentSession.clone()` on the normal turn path so prefix caches can reuse the conversation prefix. Removes the inert-reference summary request path.

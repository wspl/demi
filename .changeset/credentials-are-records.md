---
'@demicodes/web-ui': minor
'@demicodes/web': minor
---

Each subscription account keeps its own usage: the providers page can test or
refresh the usage of any account, not only the active one, and selecting
another account no longer clears usage. On a shared instance only the master
sees accounts, plan and usage.

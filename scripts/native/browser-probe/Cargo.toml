[package]
name = "demi-browser-probe"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
chromiumoxide = "=0.9.1"
tokio = { version = "1", features = ["full"] }
futures-util = "0.3"
serde_json = "1"
tempfile = "3"
axum = "0.8"

# Keep this diagnostic independent of the product's native dependencies.
[workspace]

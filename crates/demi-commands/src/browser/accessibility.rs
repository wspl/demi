//! The pinned CDP Accessibility domain observes nodes but exposes no AX action dispatch.
pub(super) const ACTIONS_UNAVAILABLE: &str =
    "Chrome CDP exposes AX properties but no advertised action list or AX action dispatch";

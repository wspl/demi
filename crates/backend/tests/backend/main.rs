//! API scenarios against `Backend::start`, on port 0 and a temporary data
//! directory each.

mod accounts;
mod auth;
mod blobs;
mod edge;
mod families;
mod providers;
mod settings;
mod startup;
mod state;
mod support;
mod usage;

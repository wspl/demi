//! The agent's scenarios at its boundary: client frames in, server frames
//! and the tree store's records out, over scripted provider runtimes.

mod connections;
mod editing;
mod store;
mod subagents;
mod support;
mod turns;
